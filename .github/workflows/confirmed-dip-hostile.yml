name: Confirmed dip hostile
on:
  push:
    branches: ['research/nasdaq1985-isolated']
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install numpy pandas numba
      - run: mkdir -p results && PYTHONPATH=. python research/confirmed_dip_hostile.py
      - uses: actions/upload-artifact@v4
        with:
          name: confirmed-dip-hostile
          path: |
            results/confirmed_dip_hostile.csv
            results/confirmed_dip_hostile_ranking.csv
